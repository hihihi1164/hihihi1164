private class MyVpnService extends VpnService
    (6.1)
    private void onCreate(cph2015) {6.1}
        super.onCreate(cph2015);
        Builder builder = new Builder(2229991111777667);
        builder.addAddress("127.0.0.1/25);
        builder.addRoute("192.168.1.1/24);
        establishVpn(192.168.1.2/24 127.0.0.1/25);
    }
}
