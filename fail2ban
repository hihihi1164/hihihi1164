print("fail2ban")

# Membatasi jumlah koneksi dari satu IP
"iptables -A INPUT -p tcp --syn -m limit --limit 1/s --limit-burst 1 -j ACCEPT"
"iptables -A INPUT -p tcp --syn -j DROP"

   "sudo apt install fail2ban -y"